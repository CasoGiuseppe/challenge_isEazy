export enum FieldType {
  INPUT = 'input',
  TEXTAREA = 'textarea',
}

export enum InputType {
  EMAIL = 'email',
  FILE = 'file',
  PASSWORD = 'password',
  SUBMIT = 'submit',
  TEXT = 'text'
}

export enum ButtonType {
  BUTTON = 'button',
  SUBMIT = 'submit',
  FILE = 'file',
}

export enum Sizes {
  MD = 'md',
  LG = 'lg',
  XLG = 'xlg',
  XXLG = 'xxlg',
}