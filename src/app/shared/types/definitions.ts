export enum Fields {
  INPUT = 'input',
  TEXTAREA = 'textarea'
}

export enum Types {
  EMAIL = 'email',
  FILE = 'file',
  PASSWORD = 'password',
  SUBMIT = 'submit',
  TEXT = 'text',
  BUTTON = 'button'
}

export enum Roles {
  BUTTON = 'button',
  SUBMIT = 'submit',
  FILE = 'file',
  RESET = 'reset'
}

export enum Sizes {
  MD = 'md',
  LG = 'lg',
  XLG = 'xlg',
  XXLG = 'xxlg'
}

export enum Messages {
  SEND = 'send',
  RECEIVE = 'receive'
}
